import { SplitWordsDirective } from './split-words.directive';

describe('SplitWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitWordsDirective();
    expect(directive).toBeTruthy();
  });
});
